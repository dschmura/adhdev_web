<div class="modal fade" id="adminUserModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <%= form_with model: User.new, url: users_path, class: "modal-content", local: true do |form| %>
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Create Admin User</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div class="form-group">
          <%= form.label :name %>
          <%= form.text_field :name, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :email %>
          <%= form.email_field :email, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :password %>
          <%= form.password_field :password, class: "form-control" %>
        </div>
        <%= form.hidden_field :terms_of_service, value: '1' %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-link" data-dismiss="modal">Cancel</button>
        <%= form.submit "Add User", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
